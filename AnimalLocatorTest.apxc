@isTest
publc static void testAnimalLocator() {
  @isTest
  public static void testAnimalLocator() {
    Test.setMock(HttpCalloutMock.class, new AnimalLocatorMock();
    String s = AnimalLocator.getAnimalNameById(1);
    system.debug('string returned: ' + s);
  }
}
